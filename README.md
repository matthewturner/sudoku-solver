# Sudoku Solver

The obligatory sudoku solver

## Running it in console

Run the following:

`./run.ps1`

Or to run when files change:

`./watch.ps1`

## Dependencies

```
pip install numpy
pip install watchdog
```
